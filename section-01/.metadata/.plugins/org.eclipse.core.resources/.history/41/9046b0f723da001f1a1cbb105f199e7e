services:
    userreg:
        container_name: userreg
        hostname: mysql        
        image: mysql:latest
        restart: always
        environment:
            - MYSQL_ROOT_PASSWORD=root
            - MYSQL_DATABASE=productdb
        ports:
            - 3305:3306    
        networks:
            - spring-network   